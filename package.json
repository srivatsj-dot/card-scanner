{
  "name": "card-scanner",
  "private": true,
  "version": "1.0.0",
  "description": "AI-powered trading card scanner — scan a card, get its value, hidden stats, a quality rating, and fair trade ideas.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix web install",
    "dev": "concurrently -n server,web -c blue,magenta \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
